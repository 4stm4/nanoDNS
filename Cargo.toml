[package]
name = "nanodns"
version = "0.3.0"
edition = "2024"
license = "AGPL-3.0"
description = "Minimal dependency-free DNS server for small local networks"

[[bin]]
name = "nanodns"
path = "src/main.rs"

[dependencies]
